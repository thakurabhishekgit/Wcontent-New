// This layout file is optional but helps structure nested routes.
// You can add specific layout elements for this section if needed in the future.
export default function MyCollabsLayout({ children }) {
  return <>{children}</>;
}
